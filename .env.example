# ---------------------------------------------------------------------------
# Vibefy environment. Copy to .env.local and fill in.
# NEVER commit a filled-in copy. tools/secret-scan.mjs will fail the build.
# ---------------------------------------------------------------------------

# --- Deployment identity ---------------------------------------------------
# Set once the primary domain is secured. See docs/OPEN_ITEMS.md.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_VERIFY_URL=http://localhost:3000/verify
NEXT_PUBLIC_BRAND_NAME=Vibefy
NEXT_PUBLIC_SUPPORT_EMAIL=support@example.invalid

# Jurisdiction variant for legal + privacy copy.
# One of: gdpr | uae_pdpl | za_popia | us. Default is the strictest baseline.
VIBEFY_JURISDICTION=gdpr

# --- Supabase --------------------------------------------------------------
# Local values are printed by `supabase start`. Production values live in the
# platform secret store, never here.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# --- Assessment runner -----------------------------------------------------
# Overrides where Playwright looks for its browser. Set this in the runner
# container image; leave it empty locally and Playwright resolves it itself.
VIBEFY_BROWSER_EXECUTABLE=

# --- Anthropic -------------------------------------------------------------
ANTHROPIC_API_KEY=
# Hard ceilings. A runaway agent loop must not be able to generate an unbounded bill.
VIBEFY_MAX_COST_PER_RUN_USD=5.00
VIBEFY_MAX_FREE_TIER_COST_PER_ACCOUNT_MONTH_USD=2.00
VIBEFY_GLOBAL_DAILY_SPEND_CAP_USD=200.00
VIBEFY_FREE_TIER_WEEKLY_BUDGET_ALERT_USD=100.00

# --- Stripe ----------------------------------------------------------------
# Without these the console says payments are not configured, and the webhook
# route falls back to a fake provider that still verifies signatures with a real
# HMAC. Production refuses to start without them.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# Stripe price ids, one per plan in config/pricing.json.
STRIPE_PRICE_ONE_OFF=
STRIPE_PRICE_CERTIFIED=
STRIPE_PRICE_AGENCY=
STRIPE_PRICE_ORGANISATION=

# --- Reports ---------------------------------------------------------------
# Where rendered reports are stored. Local disk in development; an object
# storage bucket in production — see docs/OPEN_ITEMS.md.
VIBEFY_REPORT_DIR=.tmp/reports

# --- Badge signing (M3) ----------------------------------------------------
# Ed25519 private key, PKCS#8 PEM, base64-encoded. Generated at M3 and stored
# ONLY in the platform secret store. If this leaks, every badge is forgeable.
VIBEFY_BADGE_SIGNING_KEY_B64=
VIBEFY_BADGE_KEY_ID=

# --- Email (M2) ------------------------------------------------------------
RESEND_API_KEY=
VIBEFY_EMAIL_FROM=

# --- Observability ---------------------------------------------------------
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
