# Open items

Everything stubbed, deferred or awaiting a founder decision. Anything named `STUB_` in the
codebase must appear here. Nothing leaves this list silently.

## Awaiting founder input

| Item                                                                              | Blocking                                            | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------------------------------------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Legal entity and jurisdiction                                                     | Legal drafts can be finalised, not started          | Drafts are written GDPR-grade; selecting a jurisdiction is a variant switch                                                                                                                                                                                                                                                                                                                                                                        |
| Primary domain (`vibefycode.app` / `.io` / `getvibefycode.com`)                   | Badge origin, email sending domain, OAuth callbacks | `verify.<domain>` must be a separate subdomain from the app                                                                                                                                                                                                                                                                                                                                                                                        |
| Contact email                                                                     | Legal documents, transactional email `From` address |                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Trademark search for "VibefyCode" and "Verified by VibefyCode", classes 42 and 35 | Brand spend, launch, and now the public directory   | See `BUSINESS_CHECKLIST.md`; a trust mark we do not own is not a trust mark                                                                                                                                                                                                                                                                                                                                                                        |
| **A vector original of the colour mark**                                          | Retiring the last of the reconstruction             | Largely closed on 2026-08-25. The wordmark is now the supplied letterforms, and the mono masters are traced from the supplied silhouette. What remains is the **colour** mark: its ribbon weave is carried by continuous shading, which a trace flattens, so `packages/shared/src/brand.ts` still draws it. Closing it needs the mark drawn as geometry by a person — the trace in `brand/source/traced-mark.svg` is the right thing to hand them. |

## Deferred by milestone

| Item                                       | Deferred to | Reason                                                                                             |
| ------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------- |
| Assessment engine pipeline                 | M1          | M0 is foundations only                                                                             |
| Stripe integration, invoices, tax          | M2          |                                                                                                    |
| Badge issuance, signing, verification page | M3          | The Ed25519 key pair is not generated until M3 so it is never committed to a repo that predates it |
| Continuous monitoring and drift detection  | M4          | Delivered — see MILESTONES.md                                                                      |
| Agency and organisation surfaces, SSO      | M5          | Delivered — see MILESTONES.md                                                                      |
| Expo mobile app                            | M6          | Delivered — see MILESTONES.md                                                                      |
| Public directory                           | M7          | Delivered — see MILESTONES.md. The cold-start argument stands: it is empty until customers certify |
| Marketing arm                              | M8          | Blocked on the independence policy being implemented and documented                                |

## Known gaps in shipped code

Things that work but are narrower than they will need to be. Each is here so it cannot be
mistaken for finished.

| Gap                                                                    | Where                                       | Why it is acceptable for now                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------------------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The authenticated console is not accessibility-scanned                 | `pnpm check:a11y`                           | Twelve public pages are scanned on every CI run, plus the report, the badge and the alert email in `pnpm test`. Reaching the console needs a live auth service, which the local harness does not run — so the pages a customer spends most time on are covered by the contrast gate and by review, not by axe.                                                 |
| The webhook endpoint is not registered with the provider               | Resend dashboard                            | `POST /api/email/webhook` verifies and applies bounces and complaints, and refuses everything it cannot prove came from the provider. Pointing Resend at it, and setting `RESEND_WEBHOOK_SECRET`, is an operator step that follows the domain decision.                                                                                                        |
| The journey test substitutes one stage in its second act               | `tests/journey.test.ts`                     | A loopback fixture is served over plain HTTP, which is a critical finding forever, so an application that _passes_ cannot be built out of one. The first act runs the real engine against the real fixture; the later acts substitute the observation and keep every step downstream of it. Closing this needs a fixture served over TLS.                      |
| Only the plans carry a detailed explanation                            | `apps/web/components/disclosure.tsx`        | The `Disclosure` component and the "what happens, what is not included, how to stop" shape exist and are used on every plan. The other things a customer buys into — an assessment depth, monitoring, a policy profile, the badge licence — still explain themselves in a sentence. Same component, one page at a time.                                        |
| The triage does not read the previous score from the database          | `apps/web/app/review/page.tsx`              | `triageAssessment` accepts a `previousScore` and raises a large-move flag from it, and the queue does not yet fetch one. The rule works and is tested; the queue simply passes nothing, so that one flag never fires there. A second query per assessment is the wrong fix — it wants a view.                                                                  |
| The trust check has no rate limit                                      | `/trust-check`                              | Anyone can make the server fetch a page of their choosing, and nothing stops them doing it in a loop. The request itself is tightly bounded — one GET, no private addresses, 10s, 2MB — so the exposure is our own bandwidth and somebody else's logs, not our network. A limit belongs at the edge (the hosting platform), not in a bespoke counter.          |
| The trust check reads one page, not the site                           | `packages/trustcheck`                       | A cancellation route behind a sign-in, or terms on a second page, are invisible to it — and it says so in every result. Following one level of policy links would find much more, and would need a crawl budget and a robots.txt reading that this deliberately does not have yet.                                                                             |
| Public review sources are not consulted                                | `packages/trustcheck`                       | Anré asked for reviews and reputation alongside the page check. Those come from third-party sources with their own terms, their own accuracy problems, and their own defamation exposure when quoted about a named company. Worth doing, worth doing carefully, and a separate decision from the page check.                                                   |
| Marketing services are not built                                       | M8                                          | Requested, and blocked on the same thing M8 has always been blocked on: it must sit visibly outside the wall that keeps commercial data away from scores. A rater that also sells marketing is the conflict this product exists to solve, so the separation has to be structural rather than promised.                                                         |
| SPF, DKIM and DMARC are not set up                                     | The sending domain                          | Blocked on the domain decision. Without them the notices land in spam, and a notice in spam is not a notice.                                                                                                                                                                                                                                                   |
| EAS build and store submission are not configured                      | `apps/mobile`                               | The app runs in Expo Go and a development build. `eas.json`, signing credentials and store listings need the Apple and Google accounts, which follow the legal-entity decision.                                                                                                                                                                                |
| Only the wordmark is outlined                                          | `packages/shared/src/wordmark.generated.ts` | Unchanged by the 2026-08-25 work, which replaced the wordmark's source rather than its scope. The legends — "VERIFIED BY", the status words, the tagline — are still live text with pinned widths. They are descriptive, not the mark, so a substituted font is cosmetic there. Outlining them would remove the last font dependency from the badge.           |
| Most console pages have not adopted the page vocabulary                | `apps/web/app/console/**`                   | The shapes exist (`panel`, `bar`, `stat`, `chip`, `eyebrow`) and the home page is built from them. The overview adopted them on 2026-08-26; the rest of the console's pages are still long single columns of bespoke markup. Converting them is mechanical but page by page, and worth doing against real content rather than in one sweep.                    |
| The mark simplifies the ribbon weave                                   | `packages/shared/src/brand.ts`              | Applies to the **colour** mark only since 2026-08-25 — the mono masters are traced from the artwork and carry the weave exactly. The colour version is still two strokes with a knockout, because the artwork carries the weave in shading rather than in outlines and a trace flattens it.                                                                    |
| The seal is composed from the reference renders, not derived from them | `packages/badge/src/render.ts`              | Deliberate — see decisions 158 and 159. The references carried an AI watermark, did not vectorise, and were illegible at the 96px the licence permits; they were removed from the repository on 2026-08-25 and the composition they informed is described in `packages/shared/src/brand.ts` instead.                                                           |
| The `verify.` subdomain is not deployed                                | Routing                                     | The routes exist and work; host-based routing is blocked on the domain decision.                                                                                                                                                                                                                                                                               |
| Key-compromise re-signing is manual                                    | `docs/RUNBOOK.md`                           | Planned rotation is covered by keeping retired keys published. A bulk re-signing script is not written.                                                                                                                                                                                                                                                        |
| Identity-provider registration is an operator step                     | `/console/workspace/<id>/sso`               | The domain claim, DNS verification, enforcement and sign-in routing are built. Registering the SAML/OIDC provider with the auth service is manual, and the console says so.                                                                                                                                                                                    |
| A deletion request is answered by a person, not automated              | `/review/requests`                          | The flow, the deadline and the refusal-basis requirement are built. Actually erasing an account across every table is a decision with irreversible consequences, and it is done deliberately rather than by a sweep.                                                                                                                                           |
| Only a platform admin can answer an access request                     | `/review/requests/<id>/export`              | The export is a button now, but `consents` and `data_requests` are readable only by the person or a platform admin, so a reviewer is refused rather than handed a partial file. With one admin, that admin is a single point of failure for a statutory deadline. Widening it means widening who can read everyone's consents, which is a decision, not a fix. |
| The directory is empty until something is certified                    | `/directory`                                | The cold-start the brief warned about, and the reason M7 is seventh. No code change makes it go away.                                                                                                                                                                                                                                                          |
| Domain discovery on sign-in is unauthenticated                         | `public.sso_routing`                        | It confirms that one exact domain enforces single sign-on. Every enterprise sign-in performs the same step; it reveals no organisation name, size or unenforced connection.                                                                                                                                                                                    |
| The free tier's per-run ceiling is under what a real run costs         | `pricing.ceilings.perRunCostUsd.limited`    | The first production run cost $0.53 against a $0.50 limit and aborted before it could compose a narrative. So the free tier currently produces a preview that always stops just short of a report. Raising the ceiling, shortening the free pipeline, or accepting the abort are three different products; it is a pricing decision, not a bug fix.            |
| A retry re-runs the whole paid assessment to redo a failed persist     | `apps/worker/src/main.ts`                   | Deterministic errors no longer retry at all, which removes the case that actually cost money. A transient database failure still repeats the entire run rather than just the write. Splitting run from persist would fix it and is a larger change than this incident justified.                                                                               |

## Stubs in the codebase

| Symbol       | File | Replaced by |
| ------------ | ---- | ----------- |
| _(none yet)_ |      |             |
